#include "main.h"
/**
 * add - a function that adds two integers and returns results
 * @a: first number
 * @b: Second number
 * Return: The result of the sum
 */
int add(int a, int b)
{
	return (a + b);
}

